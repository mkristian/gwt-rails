-singleton -readonly +gin
-singleton +readonly +gin
+singleton +readonly +gin
+singleton -readonly +gin
-singleton -readonly -gin
-singleton +readonly -gin
+singleton +readonly -gin
+singleton -readonly -gin

+singleton -timestamps -optimistic -readonly
+singleton +timestamps -optimistic -readonly
+singleton +timestamps +optimistic -readonly
+singleton +timestamps -optimistic +readonly
+singleton -timestamps -optimistic +readonly
-singleton -timestamps -optimistic -readonly
-singleton +timestamps -optimistic -readonly
-singleton +timestamps +optimistic -readonly
-singleton +timestamps -optimistic +readonly
-singleton -timestamps -optimistic +readonly

-optimistic -place:
+timestamps +gin +serializer
+timestamps +gin -serializer
+timestamps -gin +serializer
+timestamps -gin -serializer
-timestamps +gin +serializer
-timestamps +gin -serializer
-timestamps -gin +serializer
-timestamps -gin -serializer

+optimistic -place:
+timestamps +gin +serializer
+timestamps +gin -serializer
+timestamps -gin +serializer
+timestamps -gin -serializer

-optimistic +place:
+timestamps +gin +serializer
+timestamps +gin -serializer
-timestamps +gin +serializer
-timestamps +gin -serializer

+optimistic +place:
+timestamps +gin +serializer
+timestamps +gin -serializer
